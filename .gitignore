.env
__pycache__/
*.pyc
.venv/
venv/
src/common/fixtures/.ledger.json

# 강사 전용·비공개 초안 (학생 배포 제외)
teacher/

# 내부 준비·운영 자료 (별도 git, 공개 금지)
ops/
.DS_Store

# 로컬 전용 (공개 제외)
CLAUDE.md
.gstack/
# 내부 작업 문서 — 에이전트 규약·ADR·사용자 스토리. 수강생 대상이 아니다.
docs/

# KIS 접근토큰 캐시 (자격증명 — 커밋 금지)
.kis_token.json
.kis_last_call
