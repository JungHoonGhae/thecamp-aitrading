# 바뀐 점 (학생용)

지금 버전은 루트 `VERSION` 파일과 **같습니다.**
숙제로 적어 둔 `내-투자-판단.md` · `내-투자-스펙.md` · `.env` 는 건드리지 않습니다.

번호 읽는 법 (학생이 몰라도 됨):

| 자리 | 언제 올리나 |
|---|---|
| 앞 (`1`.1.0) | 주차·큰 덩어리가 바뀔 때 |
| 가운데 (1.`1`.0) | 숙제·안내·스킬이 바뀔 때 |
| 끝 (1.1.`0`) | 글자만 고치거나 작은 수정 |

강사가 자료를 고치면: `VERSION` 을 올리고, **이 파일 맨 위에 그 버전 칸**을 추가한 뒤
`scripts/release.sh` 를 실행한다. 태그 `v<버전>` 과 GitHub 릴리즈가 같이 생긴다.

## 학생에게 보낼 때

카톡·메일에는 아래를 그대로 보냅니다. **버전 숫자와 「한 줄」만** 고칩니다.
학생에게 `git` 명령을 시키지 않습니다. 목적어(`수업 자료`)를 빼지 마세요.

```
실습 폴더를 최신으로 맞춰 주세요. 버전 1.4.0 입니다.

AI 코딩 도구에서 실습 폴더(verify.py 가 보이는 곳)를 연 다음, 이 한 줄을 보내세요.

수업 자료 업데이트 해 줘

한 줄: (여기)
```

맞으면 🔄 상자에 **📌 버전 1.4.0** 이 보입니다. 숫자가 같으면 맞춘 겁니다.

---

## 1.4.0 — 2026-08-29

한 줄: Telegram의 TS 명령 11개와 Hermes 연결 설치가 학생 저장소에 포함됐습니다.

- `/ts_status → /ts_analyze → /ts_order_plan → 승인·보류` 흐름을 Telegram 한 곳에서 실행합니다.
- `hermes/setup_course.py`가 수업 플러그인·명령 메뉴·수업 스킬·gateway 연결을 설치합니다.
- `/ts_doctor`, `/ts_auth`, `/ts_help`에 정상 결과와 막힌 원인이 표시됩니다.
- 예약 루틴은 조회와 알림만 수행하고, 주문 계획과 승인은 Telegram의 필수 게이트로 분리했습니다.

---

## 1.3.1 — 2026-08-28

한 줄: 2부 레슨에 기대 결과와 번호 예시가 생겼고, 텔레그램 「점검」이 점검을 가리킵니다.

- 2부 레슨에 `committee.py` · 가드레일 차단 · 미국 연습 계좌 체결의 **기대 결과**를 넣었습니다.
- 제안 번호와 주문 계획 번호가 다른 이유, 초록이 탈출구를 1주차와 같은 자리에 두었습니다.
- 텔레그램 「점검」이 `/doctor` 가 아니라 `/check` 로 갑니다. `/routines` 에 참조 실험·저점고점 버튼을 다시 달았습니다.

---

## 1.3.0 — 2026-08-27

한 줄: 2주차 실습이 리밸런싱 하나에서 「나만의 시스템」으로 넓어졌습니다.

**2주차 수업 전에 한 번만 맞춰 주시면 됩니다.** 새로 설치할 것은 없습니다.
텔레그램과 예약은 수업에서 같이 붙입니다.

**한국투자증권 밖의 데이터가 붙었습니다**

- 모의투자는 **지금 값 한 점**만 줍니다. 그래서 「지금 비싼가」 「시장보다 잘했나」를 물을 수 없었습니다.
- 과거 시세·코스피·환율·공포지수·업종이 붙었습니다. 키도 설치도 필요 없습니다.
- 코인(업비트)도 같은 자리에 있습니다. 시세만 봅니다. 주문은 다루지 않습니다.
- **공식과 비공식은 무게가 다릅니다.** 야후는 문서 없는 주소라 공부용입니다.
  주문은 언제나 공식(한국투자증권)으로만 나갑니다.

**분석 네 갈래**

- 정량 — 시총 상위에서 내 스펙 ④ 로 거른 후보
- 정성 — 내 종목 뉴스 제목을 모아 AI에게 넘길 브리프
- 시장 맥락 — **내 포트폴리오가 코스피를 이겼는지**
- 업종 — 다섯 종목이 전부 같은 업종이면 분산이 아닙니다

각 파일 위쪽에 「여기를 고쳐 보세요」 칸이 있습니다. 몇 줄만 바꿔도 답이 달라집니다.

**루틴 — 정해진 시각에 혼자 도는 일**

- 아침 브리핑 · 내 전략 백테스팅 · 저점·고점 판독 · 정기 리밸런싱
- 카테고리로 위험이 이름에 보이게 했습니다. **주문이 나가는 것은 리밸런싱 하나뿐입니다.**
- 백테스팅은 내 규칙을 과거 시세에 그대로 돌려 「가만히 두기」와 견줍니다.
  기간을 바꿔서 다시 돌려 보세요. 답이 바뀌면 그건 규칙이 아니라 운이었다는 뜻입니다.

**텔레그램이 보고 채널에서 조작 창구가 됐습니다**

- 폰에서 `/check` `/balance` `/candidates` `/news` `/spec` `/routines` 를 씁니다.
- `/rebalance` 는 **승인**을 한 번 더 받고서야 주문을 냅니다.
- 봇은 **내 방에서 온 말만** 듣습니다. 노트북이 켜져 있어야 답합니다.

**예약은 시각과 사건이 다릅니다**

- 아침 브리핑처럼 시각이 정해진 것은 hermes 나 crontab 어느 쪽이든 됩니다.
- 가격 도달은 언제 올지 모릅니다. hermes 는 값이 **바뀌었을 때만** 깨웁니다.
- hermes 는 예약만 합니다. **판단은 이미 쓰고 계신 코딩 앱**(클로드·코덱스)이 합니다.

**고친 것**

- `.env` 를 그대로 복사한 분에게 뜨던 헛경고를 없앴습니다.
- 실전(`KIS_ENV=real`) 경고가 그동안 뜨지 않던 문제를 고쳤습니다.

## 1.2.0 — 2026-08-26

한 줄: 숙제를 시작하는 말이 짧아졌습니다. 긴 명령을 붙여넣지 않으셔도 됩니다.

**1주차 수업에 못 오신 분은 이 칸부터 보시면 됩니다.** 아래 두 줄이 전부입니다.

```
/environment 수업 자료 업데이트 해 줘
/homework 이번 주 숙제 절차를 안내하고, 내가 하는 걸 도와줘
```

**숙제를 시작하는 말이 짧아졌습니다**

- 전에는 긴 명령을 세 개나 차례로 붙여넣어야 했습니다. 이제 한 줄이면 됩니다.
- 클로드가 이번 주에 할 일을 먼저 보여 준 뒤, 하나씩 같이 갑니다.
- 1주차에 하실 일은 셋입니다.
  공식 도구 붙이기 → 내 규칙 적고 한 번 주문하기 → 한투 말고 다른 데이터 붙여보기.
- 셋째는 안 하셔도 됩니다. 둘째가 끝나면 이어서 할지 물어봅니다.

**회고를 따로 시키지 않습니다**

- 전에는 모의주문이 끝나면 회고 명령을 한 번 더 붙여야 했습니다. 그 자리에서 안 하고 넘어가기 쉬웠습니다.
- 이제 주문(또는 「주문 안 함」) 다음에 바로 회고로 이어집니다. 학생이 보낼 명령은 없습니다.

**한 칸만 다시 하고 싶을 때**

- 긴 명령을 없애지는 않았습니다. `lessons/1부-연결/3-1주차-숙제.md` 아래쪽에 그대로 있습니다.
- 처음 하실 때는 위의 두 줄만으로 됩니다.

**자료 맞추기 안내를 카톡에서 뺐습니다**

- 카톡 글에는 주소 한 줄만 넣습니다. 순서는 `.agents/skills/environment/references/update.md` 가 갖습니다.
- 그 글은 버전 숫자를 박아 두지 않습니다. 맞춘 뒤의 `VERSION` 과 `CHANGELOG` 맨 위 칸을 읽어서 알려 줍니다.

**새 스킬**

- `homework` — 평일 숙제를 맡습니다. 이번 주에 할 일을 먼저 보여 주고 하나씩 같이 갑니다.

**바뀌지 않은 것**

- 한투 공식 도구는 그대로 둘입니다. 코딩도우미는 둘러보기만, 트레이딩 MCP 로만 주문합니다.
- Docker 가 막히면 `kis-lecture-lab` 으로 모의주문·잔고·수익률까지 그대로 됩니다. 그것도 숙제입니다.
- `내-투자-판단.md` · `내-투자-스펙.md` · `.env` 는 건드리지 않습니다.

## 1.1.0 — 2026-08-24

한 줄: 평일 숙제 길과 스킬을 넣었습니다. 수업 자료를 한 번만 맞추시면 됩니다.

**숙제 (평일 장중, 한 번)**

- 세 줄만 순서대로 붙이면 됩니다. 자세한 건 `lessons/1부-연결/3-1주차-숙제.md`.
- 남는 것은 `내-투자-판단.md` 입니다. 2주차가 이 칸을 읽습니다.
- `.env` 의 `KIS_MODE=live` 로 모의투자에 붙습니다. 2주차 전에 `mock` 으로 되돌립니다.

**한투 공식 도구는 둘입니다 (섞으면 주문이 안 나갑니다)**

- 코딩도우미 MCP — 문서 **334개** 검색·샘플. **주문 없음.** Docker 필요 없습니다.
- 트레이딩 MCP — 호출 **166개**, 모의주문까지. **Docker 필요.**
- 수업에서 쓴 `kis-lecture-lab` 다섯 개는 그대로 둡니다. 토요일 수업은 계속 이걸 씁니다.

**Docker 가 안 되면 숙제를 못 하는 게 아닙니다**

- 설치·실행·연결은 AI가 합니다. 설치 창을 누르고 앱을 한 번 켜 주시는 것까지가 전부입니다.
- 회사 노트북 등으로 막히면 10분에서 멈추고, `kis-lecture-lab` 으로 **모의주문·잔고·수익률**
  까지 그대로 하시면 됩니다. 「공식 도구 못 붙임」 한 줄만 남기면 그것도 숙제입니다.

**공식 도구 붙이는 법을 실제로 돌려서 확인했습니다 (2026-08-24)**

- Docker 로 띄우고 조회까지 성공하는 것을 확인한 뒤 명령을 그대로 적었습니다.
- 빠뜨리면 안 되는 것 세 개를 찾았습니다. 안내에 없던 것들입니다.
  `MCP_ACCESS_TOKEN`(없으면 바로 죽음) · `MCP_HOST=0.0.0.0`(없으면 안 닿음) ·
  계좌번호 두 개(없으면 조회가 계좌를 못 찾음)
- 이미지는 약 920MB, 빌드는 2~5분 걸립니다.
- 도커 없이 붙이는 방법은 **쓰지 마세요.** 붙은 것처럼 보이는데 조회가 전부 실패합니다.

**시작 전에 30초 점검 (`숙제 환경 준비됐는지 확인해 줘`)**

- `.env` 키·`KIS_MODE`·공식 도구 설정을 미리 봅니다. 깨지고 나서 찾는 것보다 쌉니다.
- 특히 설정 파일에 한글이 남았는지 봅니다. 남아 있으면 원인이 안 보이는 에러로 죽습니다.

**스킬 역할을 갈랐습니다**

- 네 개 스킬이 같은 말에 반응해 어느 게 뜰지 몰랐습니다. 겹치는 말을 걷어냈습니다.
- 받기·맞춤·점검은 environment · 이미 깨졌으면 troubleshooting ·
  다음 단계는 assistant · 공식 도구는 kis-trading-mcp · 숙제 본체는 investment-habit-rules

**새 스킬**

- `kis-trading-mcp` — 공식 도구 구분 · Docker · 조회/액션 둘러보기 · 고르기
- `skill-vs-mcp` — 스킬과 MCP 의 차이. 사이트를 직접 열지 않고 AI가 찾습니다
- 업데이트 🔄 · 설치 📦 · 한입 🧃 · 회고 🪞 상자를 넣었습니다

**막히기 쉬운 곳을 미리 막아 두었습니다**

- `lessons/참고/kis_devlp.example.yaml` 신설 — 설정에 한글이 남으면 원인 안 보이는
  `latin-1` 에러가 납니다. 이 파일을 복사해 세 칸만 채우면 됩니다.
- 공식 트레이딩 MCP 는 **Docker + SSE** 로만 붙입니다. stdio 는 호출이 죽습니다(2026-08-24 확인).
- 예전 연동 가이드의 「수정 3가지」는 **이제 필요 없습니다.** 공식이 7/28 에 고쳤습니다.
- 토큰은 앱키당 1분에 1회입니다. **한 번에 한 손만** 쓰세요.
- Windows: 스킬은 실제 폴더입니다. `python` 을 씁니다 (`python3` 없음).

**첫 실행에서 고친 것**

- Windows 에서 주문이 미리보기에서 멈추던 문제(글자 깨짐)
- live 잔고의 현금이 정산 전 값이라 이미 산 종목을 다시 사던 문제
- 응답이 느릴 때 안내 없이 죽던 문제 (타임아웃 30초 + 안내 문구)

## 1.0.0 — 2026-08-22

한 줄: 1주차 수업 당일 자료입니다.

- 환경 스킬 · 투자 습관 스킬 · `내-투자-스펙.md` · `내-투자-판단.md`
- 수업용 `kis-lecture-lab` 다섯 개 (현재가 · 잔고 · 뉴스 · 시총 · 모의주문)
