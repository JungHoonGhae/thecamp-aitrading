# ai-trading-lab

패스트캠퍼스 강의용 실습 저장소. 학생은 Telegram에서 조회·분석·모의주문안을 요청한다.
Hermes가 작업자를 고르고, 규칙 코드가 계산하며, 사람이 승인한 모의주문만 계좌에 반영한다.

## Language

**스펙(Spec)**:
`agent/spec/*.md` — 학생이 마크다운으로 선언한 목표 비중·규칙·가드레일. 코드가 아니라 선언이며,
`내-투자-스펙.md` → `sync_spec.py` 를 통해 여기로 반영된다.
_Avoid_: 설정(config), 전략 파일

**가드레일(Guardrail)**:
스펙에 선언된, 위반 시 주문을 하드하게 차단하는 규칙(예: 한 종목 최대 비중). 경고(Warning)와
달리 실행을 막는다.
_Avoid_: 제약(constraint), 검증(validation)

**신뢰 게이트(Trust gate)**:
읽기 전용 → 미리보기 → 모의 자동 → 실전으로, 권한을 신뢰가 쌓인 만큼만 단계적으로 여는 이
강의의 핵심 설계 원칙. 토요 수업의 본편은 mock 연습 계좌(체결이 다음 조회에 보임)이고,
평일 전환은 `.env` 의 `KIS_MODE=live`, 실전은 졸업 스위치 두 줄이다.
_Avoid_: 안전장치(safety feature) — 신뢰 게이트는 완전한 안전을 단정하지 않는다.

**가설(Hypothesis)**:
일상적인 의문을 데이터로 확인할 수 있게 바꾼 질문. 2주차 정본은 「직전 한 달을 빼고
그전 1년 동안 강했던 미국 대형주는 다음 달에도 상대적으로 강할까?」다. 시스템을 처음
설계하거나 근거를 다시 검토할 때만 다루며, 주문할 때마다 새로 세우지 않는다.
_Avoid_: 매 주문의 사유 번호, AI가 매번 새로 만드는 투자 아이디어

**시스템 규칙(System rule)**:
근거와 한계를 확인한 뒤 사람이 처음 한 번 채택해 저장한 실행 규칙. 평소에는
`/ts_status → /ts_analyze [선택] → /ts_order_plan → 사람 승인` 순서로 움직인다.
`/ts_rule`은 처음 설정하거나 전제가 바뀌었을 때만 확인한다.
시장이나 전제가 크게 달라질 때만 가설 검토로 돌아간다.
_Avoid_: 주문마다 다시 채택하는 가설, AI가 자동으로 고쳐 쓰는 규칙

**인터페이스(Interface)**:
사람이 명령을 보내고 결과를 읽고 승인하는 접점. 2주차 인터페이스는 텔레그램의
**내 투자 에이전트**다. Hermes는 인터페이스가 아니라 오케스트레이션·라우팅 레이어다.
_Avoid_: 창구

**폴백(Fallback)**:
주 작업자가 한도·로그인·과부하·연결 문제로 실패할 때 다음 작업자로 넘기는 대체 경로.
Hermes의 Solar Pro 4 무료 모델이 라우팅하고, 실제 AI 검토는 Claude → Codex → Solar →
StepFun → 현재 Nous의 다른 `:free` 모델 순서로 시도한다. 판단 경로가 모두 실패하면 규칙
계산·가드레일·점검 보고만 계속하고 실패
이유를 알린다. 주문 승인 기준은 느슨해지지 않는다.
_Avoid_: 완전한 무중단 보장, 실패를 숨기고 정상 검토로 표시, 폴백에서 자동 승인

**후보(Candidate)**:
가설을 실행 가능한 숫자로 옮긴 검증 전 규칙 문장. 산다·판다·보유·점검이 적혀 있어야 표에 올라온다. 검증을 통과하지
못하면 폐기하고, 통과해 표에 올린 것만 스펙이라 부른다.
_Avoid_: 전략(학생 제목에서), 아이디어

**참조 실험(Reference experiment)**:
강사가 출처·데이터 계보·진입·청산·구현·미국/한국 결과를 확인한 **미국 대형주 후보**의 검증.
선택 규칙 정본 문장은 「**직전 한 달을 빼고 1년 성적이 상위**인 종목을 다음 한 달 같은
금액으로 담는다」 — 「최근 월 상위」라고 쓰지 않는다(정반대로 읽힌다).
한국에 같은 규칙을 쓰면 수업 참조로 쓰지 않는다. 수업용 고정 바스켓·비공식 조정주가이며
수익 보장이나 시장 전체 구성종목 백테스트가 아니다.
_Avoid_: 참조전략 실험(파일명 `routines/참조전략-실험.py` 만 잔존), 전략 카탈로그,
학생이 가설을 처음부터 설계하는 숙제, 학생 화면의 12–2 / H-12 / 롱숏 / formation / skip

**근거 읽기 용어(Evidence-reading terms)** — 학생 앞에서 이 네 개만 쓰고, 쓰기 전에 정의한다:
- **고정 바스켓**: 지금 구성 종목을 과거에도 고정해 계산한 수업용 묶음. 그래서 **생존편향**이 있다.
- **생존편향**: 살아남은 종목만 과거로 가져가 성적이 실제보다 좋아 보이는 치우침.
- **벤치마크**: 비교 기준이 되는 지수(미국 SPY · 한국 069500). 「이겼다」는 항상 이 기준 대비다.
- **비용 후**: 매수·매도 수수료를 뺀 뒤의 결과. 수업 숫자는 전부 비용 후다.

**판단 제안(Advisory proposal)**:
고정 참조 결과를 읽고 AI가 **약점 · 반대 근거 · 다음 질문**만 남기는 **읽기 전용 제안**.
종목·비중·가격·수량·주문을 만들지 않으며 `KISClient` 와 주문 함수에 닿지 않는다.
사람이 채택한 내용만 **채택 스펙**이 된다. 1주차 투자 스펙(`내-투자-스펙.md`)과는 별 파일이다.
화면의 **제안 번호**는 이 제안 한 장의 이름표다 — 아직 주문이 아니다.
_Avoid_: AI 주문, 자동 비중 변경, 가격·기업·뉴스 삼인 위원회

**채택 스펙(Adopted spec)**:
학생이 판단 제안을 채택해 확정한 2주차 스펙(`.state/*-active-spec.json`). 화면의
**채택 스펙 번호**는 채택한 표의 지문이다. 스펙이 셋이면 이렇게 가른다 —
**투자 스펙**(`내-투자-스펙.md`, 학생 선언) · **스펙 파일**(`agent/spec/`, sync 전개본) ·
**채택 스펙**(2주차 미국 참조).
_Avoid_: 확정 스펙(항목명으로는), US 스펙, 로컬 모의 스펙

**스펙 채택 게이트(Spec adoption gate)**:
AI의 판단 제안을 학생이 채택·수정·거절하는 첫 번째 사람 개입. 채택 전 제안은 주문 수량
계산에 쓰지 않는다. AI가 스스로 스펙이나 가드레일을 바꿀 수 없다.

**청산 정책(Exit policy)**:
보유 종목을 언제 줄이거나 팔지 정한 확정 스펙. 미국 대형주 후보는 **매월** 같은 점수로
다시 줄을 세워 상위 밖 종목을 교체한다. 고점 대비 -10%는 자동 매도가 아니라
재검토 알림이다. 진입·청산·안 움직인 날을 모두 기록한다.
_Avoid_: 손절 정답, AI 자동 매도, 숙제로 리밸런싱 주기를 3개월로 설계하기

**주문 계획(Order plan)**:
채택 스펙과 현재 계좌를 규칙 코드가 비교해 만든 종목·방향·수량·가격 목록. AI 문장을
파싱해서 만들지 않는다. 승인할 때는 계획 전체의 해시를 함께 묶어, 승인 뒤 내용이 바뀌면
다시 확인하게 한다. **주문 계획 번호**가 그 해시다. 제안 번호와 다른 이유의 정본 문장:
「제안은 생각이고, 주문 계획은 그 생각을 채택한 뒤 내 계좌에 맞춘 실행 목록이라 번호가
다르다. 승인은 언제나 주문 계획 번호로 한다.」

**AI 위험 검토(AI risk review)**:
규칙 코드가 만든 주문 계획을 Claude → Codex → 무료 모델 체인 순서로 읽고, 사람이 승인 전에 볼
위험만 설명하는 단계. 종목·방향·수량·지정가를 바꾸거나 주문을 승인·실행하지 않는다.
모든 AI 경로가 실패해도 규칙 계획과 사람 승인, 모의 실행은 계속된다.
_Avoid_: AI 주문 실행, AI가 승인 뒤 수량 보정, AI 실패 때문에 승인 결과 유실

**주문 승인 게이트(Order approval gate)**:
가드레일을 통과한 정확한 주문 계획을 학생이 승인·거절하는 두 번째 사람 개입. 승인은
가드레일을 우회하지 못하며, 가드레일 위반은 승인해도 차단된다.

**연습 계좌(Practice account)**:
실제 돈이 움직이지 않는 계좌의 통칭. 국내는 mock 모드의 KIS 연습 계좌, 2주차 미국 참조는
**미국 연습 계좌**(로컬 원장 `.state/us-ledger.json`)다. `mock` 은 `.env` 의 모드 값으로만 쓴다.
_Avoid_: 로컬 모의계좌, 로컬 모의, 수업용 체결

**본편(Main path)**:
수업이 기본으로 걷는 경로. 그 외는 **폴백**(본편이 막혔을 때 대신 가는 길) 또는
**선택**(안 해도 되는 확장)이라 부른다. 2주차 자동 실행 본편은 Hermes이고,
crontab·작업 스케줄러는 설치가 막힐 때의 폴백이다. 수동 텔레그램 호출은 언제나 가능하다.
6-평일 장은 선택. 한 문서 안에서 본편이라는 말을 두 가지 뜻으로 쓰지 않는다.
_Avoid_: 본편을 「숙제 본체」「미국 후보」의 뜻으로 겹쳐 쓰기

**Report**:
`common/report.py` 에 정의된, 점검·리밸런싱 결과를 담는 구조화된 데이터(dataclass). `agent.py`
가 만들고, `telegram.py` 가 평문(화면) 또는 텔레그램 메시지로 렌더링만
한다. agent.py 와 렌더러 사이의 seam — 렌더러는 Report 의 필드를 읽을 뿐, agent.py 가 만든
문자열을 다시 파싱하지 않는다(2026-07-17, 이 규칙이 깨졌을 때 실제로 버그가 났다).
_Avoid_: 보고서 문자열(report string), 메시지(message) — Report 는 문자열이 아니라 타입이다.

**Hermes**:
2주차의 **오케스트레이션·라우팅 레이어**. 특정 AI 하나에 종속되지 않게 요청을 연결된
작업자와 폴백 제공자에게 넘긴다. Nous Portal(`hermes setup --portal`) 무료 모델이 마지막
작업 경로가 된다. 또 읽기 전용 `morning-brief.py`를 정해진 시각에 시작해 결과를
Telegram의 **내 투자 에이전트**로 보낸다. 자동화 루틴은 조회와 보고만 하며,
모의주문안 생성·승인·실행은 언제나 Telegram의 수동 흐름에 남는다.
_Avoid_: Hermes 자체가 투자 정답을 낸다, Hermes가 주문을 자동 승인한다, 텔레그램을 대체한다

**졸업 스위치(Graduation switch)**:
`KIS_ENV=real` + `KIS_REAL_ACK=REAL-MONEY-OK` 이중 확인으로만 열리는, 실전(실계좌) 매매
전환 스위치. 이 강의 범위 밖이며 기본은 항상 모의투자다.
_Avoid_: 실전 모드(live mode) — live 는 mock 이 아니라는 뜻일 뿐, paper/real 을 구분하지 않는다.
