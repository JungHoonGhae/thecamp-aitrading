# 아키텍처 한눈에 보기

## 학습 흐름 (학생 여정)
```mermaid
flowchart LR
    A[README<br/>여기부터] --> S0[0-시작<br/>환경·개념]
    S0 --> C1[1부·1<br/>직접 호출]
    C1 --> C2[1부·2<br/>MCP 연결]
    C2 --> D1[2부·1<br/>예제 실행]
    D1 --> D2[2부·2<br/>스펙 수정]
    D2 --> D3[2부·3<br/>재실행·리밸런싱]
    D3 --> H[2부·4<br/>자동화 예약]
    H --> E[9-마무리<br/>신뢰 게이트]
```

## 데이터 흐름 (에이전트가 하는 일)
```mermaid
flowchart TD
    S[agent/spec 마크다운<br/>목표비중·규칙·한도] --> AG[agent.py]
    K[common/kis.py] --> AG
    K -->|mock 기본| FX[(fixtures<br/>키·휴장 무관)]
    K -->|live 선택| API[KIS 모의투자 API]
    AG -->|현재 vs 목표 비교| PV[리밸런싱 미리보기]
    PV -->|가드레일 통과 + --execute| ORD[모의 주문<br/>place_order]
    PV -->|위반 시| BLK[주문 차단]
    ORD --> K
    AG --> R[보고서 stdout]
    R --> DC[common/discord.py]
    DC -->|웹훅 있으면| DISCORD[디스코드<br/>Components v2]
    DC -->|없으면| SCR[화면 stdout]
```
> 기본은 미리보기(주문 없음). `--execute` + 가드레일 통과 시에만 모의 주문. 실계좌 아님.

## 자동화 — 에이전트를 깨우는 두 가지 방법 (둘 다 hermes)
```mermaid
flowchart LR
    subgraph 내_hermes["내 hermes-agent (무료: Nous 로그인)"]
      direction TB
      CRON["예약 (cron)<br/>--no-agent, LLM 안 씀<br/>hermes/scripts/portfolio-check.py"]
      CHAT["자연어 채팅<br/>'점검해줘' / '리밸런싱 실행해줘'<br/>hermes/skills/portfolio-check"]
    end
    CRON -->|정해진 시각| RUN[agent/agent.py 실행]
    CHAT -->|즉시| RUN
    RUN -->|stdout| DC[common/discord.py]
    DC --> DISCORD[디스코드<br/>Components v2]
```
> 예약은 "정해진 시각에 알아서", 채팅은 "지금 당장 물어보기" — 실행 로직(`agent.py`)과
> 보고 로직(`discord.py`)은 완전히 같고, 트리거만 다릅니다.

> 핵심: 지능(코드 수정·판단)은 학생의 Claude Code/Codex 가, 데이터 처리·보고는 이
> 저장소 코드가, 정기 실행·배달은 hermes 가 맡습니다. 각 층이 분리돼 있습니다.
